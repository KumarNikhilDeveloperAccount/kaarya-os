'use client';

import ComingSoon from '@/components/system/ComingSoon';

export default function CandidatesPage() {
  return (
    <ComingSoon
      title="Talent Pool"
      subtitle="Candidate discovery, ranking, and shortlisting will live here. The shell is online; the data layer will activate when the Core API is running."
    />
  );
}

